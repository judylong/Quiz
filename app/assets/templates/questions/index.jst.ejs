<h1>All Questions</h1>
<div id="questions-div">
  <% questions.each(function(question) { %>
  <div class="question">
    <p><%= question.escape('qtext') %></p>
    <p><%= question.escape('answer') %></p>
    <a href="#questions/<%= question.id %>">View</a>
  </div>
  <% }); %>
</div>
